## 绪论
1. 数据结构 = 逻辑结构+存储结构+数据运算，分清楚逻辑结构和存储结构
2. 算法：有穷，确定，可行，输入，输出
3. 时间复杂度：基本运算执行次数的数量级；空间复杂度：算法额外所需的辅助存储空间
## 线性表
1. 线性表只是一种逻辑结构，不代表存储结构。能代表的特性只有：
	+ 元素个数有限
	+ 有先后次序
	+ 每个元素数据类型相同
2. 重点关注有无头节点和尾节点，循环链表的性质，双向链表的性质